---
import Button from "../components/Button.astro"
---

<div class="hero-grid">
<section class="formular-grid slide-in-right">
        <form name="kontakt" method="post" action="https://dokument24.dk/dok/opret-testamente" netlify>
          <!-- Formular-indhold -->
                 <h2>OPRET TESTAMENTE</h2>
                 <p>Støt en ren klode for fremtidige generationer ved at inkludere Plastic Change i dit testamente. Opret det gratis hos Dokument 24 og start allerede her.</p>
          <div class="form-felt">
            <label for="full-name">Fornavn</label>
            <input placeholder="Benny" id="full-name" name="fname" type="text" required />
          </div>

          <div class="form-felt">
            <label for="full-name">Efternavn</label>
            <input placeholder="Andersen" id="full-name" name="fname" type="text" required />
          </div>

          <div class="form-felt">
            <label for="email">E-mail</label>
            <input placeholder="din@mail.com" id="email" name="email" type="email" required />
          </div>

          <div class="form-control">
            <div class="checkbox-container">
              <input type="checkbox" id="brev" name="interest" value="kodning" >
              <label for="brev">Tilmeld Plastic Changes nyhedsbrev</label>
            </div>
          </div>


<div class="knap">
          <Button variant="gratis" tekst= "Opret gratis testamente"/>
        </div>
        </form>
      </section>
    </div>

      <style>
.checkbox-container {
  display: flex; /* Use flexbox to align items horizontally */
  align-items: center; /* Center items vertically */
}

.checkbox-container input {
  margin-right: 10px; /* Add some spacing between the checkbox and label */
}

form {
  display: grid;
  padding-top:  var(--spacing-infocard-ikon);
  padding-bottom:  var(--spacing-infocard-ikon);
  padding-left:  var(--spacing-infocard-ikon);
  padding-right:  var(--spacing-infocard-ikon);
  background: hsl(0, 0%, 100%);
  border-radius: 1.9375rem;
  border: 2px solid var(--contrast-500-color);
  gap: 20px;
  margin-top: 5rem;
  margin-inline: 3rem;
}

.form-felt {
  display: grid;
  font-size:0.875rem;
 
}

/*** gør så det rykker sig på næste linje ***/
label,
input{
  display: block;
}


/* == Body styles == */
body {
  line-height: 1.6;
}

input[type=text] {
  width: 100%;
  padding: 12px 20px;
  margin: 8px 0;
  box-sizing: border-box;
  border: 1.5px solid var(--grey-500-color);
  -webkit-transition: 0.5s;
  transition: 0.5s;
  outline: none;
}

input[type=text]:focus{
  border: 1.5px solid var(--grey-900-color); 
}

input[type=email] {
  width: 100%;
  padding: 12px 20px;
  margin: 8px 0;
  box-sizing: border-box;
  border: 1.5px solid var(--grey-500-color);;

  outline: none;
}

input[type=email]:focus{
  border: 1.5px solid var(--grey-500-color);
}

h2{
  font-family: "Campton-boldDEMO";
  font-size: 1.50rem;

}

p{
  color: var(--grey-500-color);
  font-size: 0.75rem;

}

.knap{
  align-items: center;
  text-align: center;
}

.form-control{
  font-size:0.80rem;
}
#brev{
  accent-color: var(--contrast-500-color);
}

@media (min-width: 1000px) {


  form {
    display: grid;
    grid-template-columns: 1fr;
    padding: var(--spacing-forumlar);
    background: hsl(0, 0%, 100%);
    border-radius: 0.75rem;
    border: 3px solid var(--contrast-500-color);
    gap: 20px;

  }

  .form-felt {
    display: grid;
    font-size: 1rem;
   
  }

  label,
  input {
    display: block;
  }

  input[type="text"],
  input[type="email"] {
    width: 100%;
    padding: 8px 12px;
    margin: 3px 0;
    box-sizing: border-box;
    border: 1.5px solid var(--grey-500-color);
    -webkit-transition: 0.5s;
    transition: 0.5s;
    outline: none;
  }

  input[type="text"]:focus,
  input[type="email"]:focus {
    border: 1.5px solid var(--grey-900-color);
  }

  h2 {
    font-family: "Campton-boldDEMO";
  
    font-size: 1.9rem;
  }

  p {
    color: var(--grey-500-color);
    font-size: 0.89rem;
  }

  .knap {
    align-items: center;
    text-align: center;
  }
}

.slide-in-right{animation:slide-in-right 1.5s cubic-bezier(.25,.46,.45,.94) both}


@keyframes slide-in-right{0%{transform:translateX(1000px);opacity:0}100%{transform:translateX(0);opacity:1}}
      </style>